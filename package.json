{
  "name": "assertquest",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:start": "node scripts/db.mjs start",
    "db:stop": "node scripts/db.mjs stop",
    "db:status": "node scripts/db.mjs status",
    "db:portal": "npm run studio --workspace apps/api",
    "db:psql": "node scripts/psql.mjs",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:th-web": "npm run dev --workspace apps/th-web",
    "build": "npm run build --workspace packages/shared && npm run build --workspace apps/api && npm run build --workspace apps/web && npm run build --workspace apps/th-web",
    "test": "npm run test --workspace apps/api",
    "typecheck": "npm run typecheck --workspace packages/shared && npm run typecheck --workspace apps/api && npm run typecheck --workspace apps/web && npm run typecheck --workspace apps/th-web",
    "lint": "npm run lint --workspace apps/api && npm run lint --workspace apps/web && npm run lint --workspace apps/th-web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
